'use client';
import Link from 'next/link';
import { useState } from 'react';
export default function RegisterPage() { const [message, setMessage] = useState(''); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:5000'}/api/auth/register`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ name: form.get('name'), email: form.get('email'), password: form.get('password') }) }); const data = await response.json(); if (response.ok) { localStorage.setItem('accessToken', data.accessToken); window.location.href = '/profile'; } else setMessage(data.error ?? 'Unable to register'); } return <main className="site-shell"><div className="container"><header className="header"><Link href="/" className="brand"><span className="brand-mark">✦</span>videxpulse<span style={{ color: 'var(--brand)' }}>.</span></Link><Link href="/login" className="text-link">Sign in</Link></header><section className="section" style={{ maxWidth: 500, margin: '35px auto' }}><div className="eyebrow">Join the marketplace</div><h1 style={{ fontFamily: 'Plus Jakarta Sans', fontSize: '2.5rem', letterSpacing: '-.06em' }}>Create account</h1><form onSubmit={submit} style={{ display: 'grid', gap: 14, marginTop: 25 }}><input className="search-input" name="name" required minLength={2} placeholder="Full name" style={{ border: '1px solid var(--line)', borderRadius: 12 }} /><input className="search-input" name="email" required type="email" placeholder="Email address" style={{ border: '1px solid var(--line)', borderRadius: 12 }} /><input className="search-input" name="password" required minLength={8} type="password" placeholder="Password (8+ characters)" style={{ border: '1px solid var(--line)', borderRadius: 12 }} /><button className="button button-primary" type="submit">Create account</button>{message && <p style={{ color: 'var(--brand-dark)' }}>{message}</p>}</form></section></div></main>; }
