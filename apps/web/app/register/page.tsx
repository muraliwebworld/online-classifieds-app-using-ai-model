'use client';
import Link from 'next/link';import{useState}from'react';
export default function RegisterPage(){const[message,setMessage]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:5000'}/api/auth/register`,{method:'POST',credentials:'include',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),password:f.get('password')})});const d=await r.json();if(r.ok){localStorage.setItem('accessToken',d.accessToken);location.href='/profile'}else setMessage(d.error??'Unable to register')}return <main className="site-shell"><div className="container"><header className="header"><Link href="/" className="brand">✦ videxpulse.</Link><Link href="/login" className="text-link">Sign in</Link></header><section className="section" style={{maxWidth:500,margin:'35px auto'}}><div className="eyebrow">Join the marketplace</div><h1>Create account</h1><form onSubmit={submit} style={{display:'grid',gap:14,marginTop:25}}><input className="search-input" name="name" required minLength={2} placeholder="Full name"/><input className="search-input" name="email" required type="email" placeholder="Email address"/><input className="search-input" name="password" required minLength={8} type="password" placeholder="Password (8+ characters)"/><button className="button button-primary">Create account</button>{message&&<p>{message}</p>}</form></section></div></main>}
